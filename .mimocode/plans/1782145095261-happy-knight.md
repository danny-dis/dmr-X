# Plan: Fix issues, verify readiness, and start local dev server

## Goal
Fix build/lint issues, verify the project is ready to ship, and start the full dev environment.

## Issues Found

### Build Failure (Critical)
- **Root cause**: `apps/ui/vite.config.ts` has duplicate `build` keys (lines 12 and 25). The second block overrides the first, losing `target: 'es2022'`. esbuild falls back to older browser targets (chrome87/es2020) which can't handle JS destructuring.
- **Fix**: Merge the two `build` blocks into one with `target: 'es2022'`, `outDir`, and `emptyOutDir`.

### Lint Error
- **1 error**: Empty block at `apps/ui/src/lib/api.ts:121`
- **1358 warnings**: 950 import ordering (auto-fixable), 250 unused vars, 120 no-console in CLI

### Tests
- All 44 unit test files pass (420+ assertions green)
- Windows-specific tinypool flakiness (`Channel closed`) — not a logic failure

## Steps

### 1. Fix vite.config.ts duplicate build key
- File: `apps/ui/vite.config.ts`
- Merge lines 12-14 and 25-28 into a single `build` block:
  ```ts
  build: {
    target: 'es2022',
    outDir: '../gateway/public',
    emptyOutDir: true,
  },
  ```

### 2. Fix lint error (empty block)
- File: `apps/ui/src/lib/api.ts:121`
- Add a comment or handle the empty block

### 3. Auto-fix import ordering warnings
- Run `bun run lint:fix` to auto-fix the ~950 import ordering warnings

### 4. Verify build succeeds
- Run `bun run build` — all packages including UI should build

### 5. Verify lint clean
- Run `bun run lint` — should have 0 errors, reduced warnings

### 6. Verify tests still pass
- Run `bun run test` — confirm no regressions

### 7. Start full dev environment
- Run `bun run dev` (turbo dev) — starts gateway (:3000) + UI (:4200)

## Files to Modify
1. `apps/ui/vite.config.ts` — merge duplicate build blocks
2. `apps/ui/src/lib/api.ts` — fix empty block at line 121

## Verification
- `bun run build` succeeds
- `bun run lint` has 0 errors
- `bun run test` passes
- `bun run dev` starts both services
- Gateway accessible at http://localhost:3000
- UI accessible at http://localhost:4200
