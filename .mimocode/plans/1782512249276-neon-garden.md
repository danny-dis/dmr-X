# Plan: Fix All Preexisting Errors

## Summary
Fix all 21 TypeScript errors and 5 ESLint errors discovered in the codebase. Warnings (877) are out of scope per the user's "fix all preexisting errors" request — errors only.

## Errors by Category

### Category 1: `@dmr-x/cache` module not found (10 TS errors)
**Files:** `3d.routes.ts`, `audio-separation.routes.ts`, `audio.routes.ts` (2), `chat.routes.ts` (3), `embeddings.routes.ts`, `images.routes.ts`, `moderation.routes.ts`, `ocr.routes.ts`, `rerank.routes.ts`, `video.routes.ts`

**Root cause:** These route files import from `@dmr-x/cache` but TypeScript can't find the module or its type declarations. The source exists at `services/cache/src/cache.service.ts` but the package likely lacks proper `package.json` exports or hasn't been built.

**Fix approach:**
1. Check `services/cache/package.json` for `main`/`types`/`exports` fields
2. Build the cache package: `bun run build` in `services/cache/` or via turbo
3. If no build script exists, add one and ensure `dist/` has type declarations
4. Verify the import paths in route files match the package's exported API

### Category 2: `verifyApiKey` and `hashApiKeyWithSalt` not on `@dmr-x/utils` type (3 TS errors)
**Files:** `auth.middleware.ts:5`, `admin.routes.ts:888,2564`

**Root cause:** These functions exist in `packages/utils/src/crypto.ts` but aren't exported in the compiled `dist/index.js` types. Gateway resolves to `dist/index` not source.

**Fix approach:**
1. Check `packages/utils/src/index.ts` exports — ensure `verifyApiKey` and `hashApiKeyWithSalt` are exported
2. Rebuild `packages/utils`: `bun run build` in packages/utils
3. Verify the exports appear in `packages/utils/dist/index.d.ts`

### Category 3: Implicit `any` type on parameter `b` (4 TS errors)
**Files:** `embeddings.routes.ts:32,69`, `images.routes.ts:36,71`

**Fix approach:**
1. Read the surrounding code to determine the correct type for parameter `b`
2. Add explicit type annotation (likely a buffer or response type)

### Category 4: `NamespacedCache.keys` method missing (2 TS errors)
**File:** `services/cache/src/cache.service.ts:248,249`

**Root cause:** Code calls `.keys()` on a `NamespacedCache` instance but the wrapper only exposes `keysByPrefix()`.

**Fix approach:**
1. Either add a `keys()` method to `createNamespacedCache()` in `packages/db/src/cache.ts`
2. Or refactor the callers in `cache.service.ts` to use `keysByPrefix('')` or similar

### Category 5: Generator function without `yield` (2 TS errors)
**Files:** `pollinations-images.adapter.ts:86`, `pollinations-images.adapter.js:60`

**Fix approach:**
1. Read the generator function to understand if it should use `yield` or be a regular `async` function
2. If it returns a single value, convert from `async *` to `async`
3. If it should yield, add appropriate yield statements

### Category 6: Unsafe optional chaining (1 ESLint error)
**File:** `tests/e2e/stress-test.test.js:88`

**Fix approach:** Add parentheses or restructure the expression to avoid short-circuit with undefined throwing TypeError.

### Category 7: Empty block statements (2 ESLint errors)
**Files:** `admin.routes.ts:4566`, `admin.routes.js:3668`

**Fix approach:** Add a comment inside the empty catch/if block explaining why it's intentionally empty, or add appropriate handling.

## Implementation Order

1. **Build packages first** — Run `bun run build` from root to ensure all packages are compiled
2. **Fix Category 1** — `@dmr-x/cache` exports/types (highest impact, 10 errors)
3. **Fix Category 2** — `@dmr-x/crypto` exports (3 errors)
4. **Fix Category 3** — Add type annotations for `b` parameter (4 errors)
5. **Fix Category 4** — Add `keys()` to NamespacedCache or refactor callers (2 errors)
6. **Fix Category 5** — Generator function fix (2 errors)
7. **Fix Category 6-7** — ESLint errors (3 errors)

## Verification

After all fixes:
1. Run `bunx tsc --noEmit` — expect 0 errors
2. Run `bun run lint` — expect 0 errors (warnings acceptable)
3. Run `bun run test:unit` — verify no regressions
