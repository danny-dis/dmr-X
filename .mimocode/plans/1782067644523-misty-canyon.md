# Plan: Commit Pre-Existing Changes

The pre-release audit is complete and committed (`756045b`). 9 pre-existing files remain unstaged. Commit them as a separate commit.

## Changes to commit

1. `git add AGENTS.md CLAUDE.md apps/gateway/src/main.ts apps/gateway/src/server.ts apps/ui/src/lib/admin.ts apps/ui/src/pages/Memory.tsx apps/ui/src/pages/Settings.tsx services/mcp-server/src/server.ts services/router/src/router.service.ts`
2. `git commit` with message describing: removed dead code, replaced window.confirm with AlertDialog, fixed Settings field mappings, MCP server type fixes

## Verification
- `git status` should show clean working tree (except `.mimocode/` untracked dir)
